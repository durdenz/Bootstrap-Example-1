document.addEventListener("DOMContentLoaded", (event) => {
    // gsap code here!
      gsap.to(".card", {
      rotation : 360,
      duration : 2,
      repeat: -1,
      repeatDelay: .25,
      })
   });